pipeline {
    agent any

    tools {
        maven 'Maven-3.9.6'
        jdk 'OpenJDK-23'
    }

    stages {
        stage ('Build Project') {
            steps {
                sh 'mvn clean package'
            }
        }
    }
}